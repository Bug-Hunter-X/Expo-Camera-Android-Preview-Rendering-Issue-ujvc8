This bug occurs when using the Expo Camera API with a custom camera component. The preview doesn't render correctly on Android devices, showing a blank screen or a distorted image.  The issue seems specific to certain Android versions and devices. iOS functions correctly.